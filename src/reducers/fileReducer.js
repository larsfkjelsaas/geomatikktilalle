
const initialState = {
    result: 1,
    lastValues: []
  };
  
 export const fileReducer = (state = initialState, action) => {
    switch (action.type) {
      case "FILES_UPLOADED":
        break;
      default:
        break;
    }
    return state;
  };